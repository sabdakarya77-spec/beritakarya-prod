/**
 * SectionSeparator — konsisten separator antar zona homepage.
 * Semua layout pakai style yang sama.
 */

export function SectionSeparator() {
  return (
    <div className="border-t border-gray-100 dark:border-white/5" />
  )
}
